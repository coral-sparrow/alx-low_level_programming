#include "main.h"

/**
 * _isdigit - returns if the char is Upper case
 * @c:  integer
 *
 * Return: largest number
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
