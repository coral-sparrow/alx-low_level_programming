#include "main.h"

/**
 * _isupper - returns if the char is Upper case
 * @c:  integer
 *
 * Return: largest number
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
